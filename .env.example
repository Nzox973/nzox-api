# Variables d'environnement — copier ce fichier en .env et remplir les valeurs

# Clé secrète JWT — NE JAMAIS utiliser cette valeur en production
# Générer une clé sécurisée : openssl rand -hex 32
SECRET_KEY=changez-cette-cle-secrete-en-production-utilisez-openssl-rand-hex-32
